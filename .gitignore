# Vim
*~
*.sw[p_]

# IntelliJ IDEA
*.idea

# Sublime Text
*.sublime-project
*.sublime-workspace

# Ruby Gem
*.gem
.bundle
Gemfile.lock
**/vendor/bundle

# Node.js and NPM
node_modules
npm-debug.log*
package-lock.json
codekit-config.json

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# Linux
.directory
.Trash-*

# Temporary files
tmp/
*.tmp
*.temp

# API keys / secrets
.env

# PDF 원본은 로컬에만 임시로 두고 커밋하지 않는다
# (pdf_to_post.py가 처리 후 자동 삭제)
_papers/*.pdf

# oh-my-claudecode / bkit 로컬 상태 파일 (커밋 불필요)
.omc/
docs/.pdca-snapshots/
docs/.pdca-status.json
docs/.bkit-memory.json

# Python 캐시
scripts/__pycache__/
*.pyc

# Jekyll generated files
.jekyll-cache
.jekyll-metadata
.sass-cache
_asset_bundler_cache
_site
.gstack/

# 강의자료 큐레이션 런타임 작업 디렉토리 (커밋 불필요)
_workspace/

# Pretendard fonts (download via scripts/gen_lecture_cover.py header instructions)
.fonts/

# 네이버 크로스포스팅 로그인 프로필·디버그 스크린샷 (쿠키 포함 - 절대 커밋 금지)
scripts/.naver_profile/
scripts/.naver_shots/
scripts/naver_task.log
